<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Workout-Plan</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
</head>
<body>
    <div class="container">
        <h2>Workout-Plan</h2>
        <!-- Formular für neuen Trainingstag -->
        <form action="{{ url_for('workout_plan') }}" method="post" class="mb-4">
            <div class="form-group">
                <label for="day_name">Trainingstag:</label>
                <select class="form-control" id="day_name" name="day_name">
                    <option value="">Wähle einen Tag...</option>
                    <option value="Montag">Montag</option>
                    <option value="Dienstag">Dienstag</option>
                    <!-- Füge weitere Tage hinzu -->
                </select>
            </div>
            <div class="form-group">
                <label for="exercise_name">Trainingsname:</label>
                <input type="text" class="form-control" id="exercise_name" name="exercise_name" placeholder="Trainingsname">
            </div>
            <div class="form-group">
                <label for="sets">Anzahl Sätze:</label>
                <input type="number" class="form-control" id="sets" name="sets" placeholder="Anzahl Sätze">
            </div>
            <div class="form-group">
                <label for="repetitions">Anzahl Wiederholungen:</label>
                <input type="number" class="form-control" id="repetitions" name="repetitions" placeholder="Anzahl Wiederholungen">
            </div>
            <div class="form-group">
                <label for="weight">Gewicht:</label>
                <input type="text" class="form-control" id="weight" name="weight" placeholder="Gewicht">
            </div>
            <button type="submit" class="btn btn-primary">Hinzufügen</button>
        </form>
        
        <!-- Anzeigen vorhandener Trainingstage und Inhalte -->
        <div id="workouts">
            <!-- Beispiel für einen vorhandenen Trainingstag -->
            <!-- Diese Einträge sollten dynamisch auf Basis der Datenbankinhalte erzeugt werden -->
            <!-- Für jedes WorkoutDay-Objekt im Backend:
            <div class="workout-day">
                <h3>{{ workout_day.day_name }}</h3>
                <ul>
                    <!-- Für jedes Workout-Objekt in workout_day.workouts:
                    <li>{{ workout.exercise_name }}: {{ workout.sets }} Sätze von {{ workout.repetitions }} mit {{ workout.weight }}kg</li>
                    <!-- Bearbeiten/Löschen Buttons hier -->
                </ul>
            </div>
            -->
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
